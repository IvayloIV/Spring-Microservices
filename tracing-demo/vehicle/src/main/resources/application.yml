server:
  port: 8081

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG

spring:
  application:
    name: vehicle
  zipkin:
    baseUrl: http://localhost:9411
